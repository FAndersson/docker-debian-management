[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
GitPython = "^3.1.31"
pygithub = "^1.58.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
